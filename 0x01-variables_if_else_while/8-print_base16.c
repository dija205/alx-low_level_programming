#include <stdio.h>

/**
 * main - Prints numbers between 0 to 9 and letters between a to f.
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
	int j;

	for (j = 48; j < 58; j++)
	{
		putchar(i);
	}
	for (j = 97; j < 103; j++)
	{
		putchar(i);
	}
	putchar('\n');
	return (0);
}
